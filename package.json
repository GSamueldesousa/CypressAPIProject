{
  "name": "cypress-api-tests",
  "version": "1.0.0",
  "description": "API tests using Cypress",
  "main": "index.js",
  "scripts": {
    "test": "cypress open"
  },
  "devDependencies": {
    "cypress": "^10.0.0"
  }
}
